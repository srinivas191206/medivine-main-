# Environment Variables for ChatGPT Integration

# Add this to your .env.local file (create if it doesn't exist)
VITE_OPENAI_API_KEY=your_chatgpt_api_key_here

# Note: In production, use proper environment variable management
# Never commit API keys to version control